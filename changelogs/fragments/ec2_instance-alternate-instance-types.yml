---
minor_changes:
  - ec2_instance - add ``alternate_instance_types`` parameter to automatically retry with alternate instance types when the primary instance type fails with ``InsufficientInstanceCapacity`` error.
