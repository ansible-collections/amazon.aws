bugfixes:
  - ec2_metadata_facts: Fix parsing of tags with dashes in the name. Before, such tags would be reported in the "instance_tags_keys" dictionary, but they would be missing from the "instance_tags" one.
