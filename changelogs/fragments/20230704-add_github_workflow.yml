trivial:
  - "Add sanity, units and linters and changelog GitHub workflows."
  - "Fix linting and validation errors across several modules."

bugfixes:
  - cloudwatch_metric_alarm - Fix ``dimensions`` parameter type. It should be a list of dictionaries while it was defined as a dictionary (https://github.com/ansible-collections/amazon.aws/pull/1645).
