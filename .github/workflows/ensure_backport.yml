name: Backporting

on:
  pull_request:
    types:
      - labeled
      - unlabeled
    branches:
      - main

jobs:
  ensure:
    permissions:
      pull-requests: write
    uses: ansible-network/github_actions/.github/workflows/ensure_backporting.yml@main