---
vpc_name: "{{ resource_prefix }}-vpc"
vpc_seed: "{{ resource_prefix }}"
vpc_cidr: "10.{{ 256 | random(seed=vpc_seed) }}.0.0/16"
subnet_cidr: "10.{{ 256 | random(seed=vpc_seed) }}.{{ 256 | random(seed=vpc_seed ~ '-subnet') }}.0/24"
