---
# NOTE: This is a runme.sh based test.
# Because runme.sh bypasses ansible-test's normal role execution,
# meta/main.yml dependencies are NOT automatically executed.
# Dependencies must be declared in the aliases file using needs/target/*
# and then either explicitly included in runme.sh or added to a role's dependencies.
dependencies: []
