time=12m

# non_local: Coverage is not installed or configured on remote instances.
# This prevents ansible-test from trying to enable coverage where it will not work.
non_local

cloud/aws

needs/target/connection
needs/target/setup_connection_aws_ssm

connection_aws_ssm

unstable
