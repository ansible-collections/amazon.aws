---
# defaults file for s3
bucket_name: '{{ tiny_prefix }}-s3-object'
bucket_name_acl: "{{ bucket_name + '-with-acl' }}"
bucket_name_with_dot: "{{ bucket_name + '.bucket' }}"
bucket_name_multipart: "{{ bucket_name + '-multipart' }}"
bucket_name_mp_versioned: "{{ bucket_name + '-multipart-ver' }}"
